import { StyleSheet } from "react-native"
//for its css make an object like this and can use styelshwwt.create to optimize css this is external css 
const styles=StyleSheet.create({
  card:{
    backgroundColor:'gray'
  }
})